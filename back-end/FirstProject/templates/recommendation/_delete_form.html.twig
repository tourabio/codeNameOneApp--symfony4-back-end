<form method="post" action="{{ path('recommendation_delete', {'id': recommendation.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ recommendation.id) }}">
    <button class="btn">Delete</button>
</form>
